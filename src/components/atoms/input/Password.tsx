import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const CInputPassword = () => {
  return (
    <View>
      <Text>CInputPassword</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CInputPassword;
